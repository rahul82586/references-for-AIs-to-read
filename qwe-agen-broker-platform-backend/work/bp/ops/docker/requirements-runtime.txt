# Runtime dependencies for the API server image (M5).
#
# Deliberately NOT `pip install -e .`: pyproject carries the full development
# surface (grpcio, pandas, clickhouse, kafka, locust...) and the startup chain
# was verified to import none of them. Verified by poisoning sys.modules and
# importing api.main - keep this list honest the same way when adding imports.
#
# Test-only extras (pytest, hypothesis, aiosqlite, httpx...) live in pyproject.

# Web & API
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0
pydantic>=2.5.3

# Market data (M10): trade-server WS frames are msgpack-encoded. The feed
# adapter imports it lazily per frame, so api.main imports fine without it -
# but MARKET_DATA_SOURCE=trade_server cannot run without it.
msgpack>=1.0.7

# Database & cache
sqlalchemy>=2.0.25
alembic>=1.13.1
asyncpg>=0.29.0
greenlet>=3.0.0
redis>=5.0.1

# Security
pyjwt>=2.8.0
cryptography>=42.0.0
passlib[bcrypt]>=1.7.4
argon2-cffi>=23.1.0
pyotp>=2.9.0

# CLI & ops
typer>=0.9.0
rich>=13.7.0
psutil>=5.9.0

# Utilities
pyyaml>=6.0.1
python-dotenv>=1.0.0
structlog>=24.1.0
